const path = require('path');
const fs = require('fs');

const copyfiles = require('copyfiles');
const eslint = require('eslint');
const prettier = require('prettier');
const uglify = require('uglifyjs-folder');

